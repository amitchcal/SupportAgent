import { requireUser } from "@/lib/auth";import { assertCan,scopeTenant } from "@/lib/rbac";import { readDatabase } from "@/lib/store";import { filterAuditLogs } from "@/lib/enterprise";
export default async function AuditPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const actor=await requireUser();assertCan(actor.role,"audit:read");const query=await searchParams;const database=await readDatabase();const tenantId=scopeTenant(actor.role,actor.tenantId,query.tenantId??(actor.role==="SUPER_ADMIN"?database.tenants[0]?.id:undefined));const logs=filterAuditLogs(database,tenantId,query);return <main className="content"><a href="/admin">← Admin</a><h1>Audit history</h1><p className="muted">Read-only tenant audit records.</p><form className="panel report-filters"><label>Actor<input name="actor" defaultValue={query.actor}/></label><label>Action<input name="action" defaultValue={query.action}/></label><label>Entity<input name="entity" defaultValue={query.entity}/></label><label>From<input type="date" name="from" defaultValue={query.from}/></label><label>To<input type="date" name="to" defaultValue={query.to}/></label><button className="primary">Search logs</button></form><section className="panel">{logs.map(item=><div className="audit-row" key={item.id}><span><b>{item.action}</b><br/><span className="muted">{item.actorUserId} · {item.entityType}</span></span><time>{new Date(item.createdAt).toLocaleString()}</time></div>)}</section></main>}
